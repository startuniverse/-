# 开发环境配置
  spring:
    datasource:
      url: jdbc:mysql://localhost:3306/education_platform?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false
      username: root      # ← 修改为您的MySQL用户名
      password: 123456  # ← 修改为您的MySQL密码

  redis:
    host: localhost
    port: 6379

logging:
  level:
    com.education.platform: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG

app:
  security:
    ignore-urls:
      - /**
