<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.education.platform.mapper.UserMapper">

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultType="com.education.platform.entity.User">
        SELECT * FROM user WHERE username = #{username} AND status = 1 AND deleted = 0
    </select>

    <!-- 根据用户ID查询角色列表 -->
    <select id="selectRolesByUserId" resultType="java.lang.String">
        SELECT r.role_code
        FROM role r
        INNER JOIN user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

    <!-- 根据用户ID查询权限列表 -->
    <select id="selectPermissionsByUserId" resultType="java.lang.String">
        SELECT DISTINCT p.permission_code
        FROM permission p
        INNER JOIN role_permission rp ON p.id = rp.permission_id
        INNER JOIN user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND p.status = 1
    </select>

    <!-- 根据学校ID查询用户列表 -->
    <select id="selectBySchoolId" resultType="com.education.platform.entity.User">
        SELECT * FROM user WHERE school_id = #{schoolId} AND status = 1 AND deleted = 0
    </select>

    <!-- 为用户分配角色 -->
    <insert id="assignRoleToUser">
        INSERT IGNORE INTO user_role (user_id, role_id) VALUES (#{userId}, #{roleId})
    </insert>

    <!-- 根据角色编码查询角色ID -->
    <select id="selectRoleIdByCode" resultType="java.lang.Long">
        SELECT id FROM role WHERE role_code = #{roleCode} AND deleted = 0
    </select>

</mapper>
