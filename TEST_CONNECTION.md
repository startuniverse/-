# 连接测试指南

## 🔍 快速诊断

### 1. 检查后端服务状态

访问以下 URL，确认后端正在运行：

**方法 A：浏览器访问**
```
http://localhost:8081/swagger-ui.html
```

**方法 B：命令行测试**
```bash
# Windows PowerShell
Invoke-WebRequest -Uri "http://localhost:8081/api/v1/auth/login" -Method POST

# 或使用 curl
curl -X POST http://localhost:8081/api/v1/auth/login
```

**预期结果**：应该返回 JSON 错误（因为没有提交数据），说明接口存在。

### 2. 检查数据库连接

在 MySQL 中执行：
```sql
USE education_platform;
SELECT COUNT(*) as user_count FROM user;
```

### 3. 检查 admin 用户是否存在

```sql
SELECT id, username, real_name, status FROM user WHERE username = 'admin';
```

如果不存在，运行：
```bash
mysql -u root -p education_platform < E:\shixun\backend\database\create_admin.sql
```

### 4. 检查前端配置

查看当前生效的配置：
```bash
# 在前端目录查看 .env 文件
type E:\shixun\frontend\.env
```

应该显示：
```
VITE_API_BASE_URL=http://localhost:8081/api/v1
```

### 5. 重启前端服务

如果修改了配置，必须重启前端：

```bash
# 停止当前服务 (Ctrl + C)
# 重新启动
cd E:\shixun\frontend
npm run dev
```

## 🎯 完整测试流程

### 第一步：确保后端运行
```
✅ 后端端口：8081
✅ 上下文路径：/api/v1
✅ 数据库连接正常
```

### 第二步：创建测试用户
```bash
mysql -u root -p education_platform < E:\shixun\backend\database\create_admin.sql
```

### 第三步：重启前端
```bash
cd E:\shixun\frontend
npm run dev
```

### 第四步：浏览器测试
1. 访问 http://localhost:3000/
2. 打开浏览器开发者工具 (F12)
3. 切换到 Network 标签
4. 输入用户名：`admin`，密码：`admin123`
5. 点击登录
6. 观察 Network 中的请求

## 📊 预期的请求流程

```
浏览器 → http://localhost:3000/
  ↓
Vite 代理 → http://localhost:8081/api/v1/auth/login
  ↓
后端处理 → 返回 JWT Token + 用户信息
  ↓
前端存储 → Token 保存到 localStorage
  ↓
跳转 → /admin/dashboard
```

## 🚨 常见问题排查

### 问题 1：404 Not Found
**原因**：端口或路径不匹配
**解决**：
- 检查后端是否运行在 8081
- 检查前端 .env 配置
- 检查 vite.config.js 代理配置

### 问题 2：500 Internal Server Error
**原因**：数据库连接失败或 SQL 错误
**解决**：
- 检查 MySQL 是否运行
- 检查数据库 `education_platform` 是否存在
- 查看后端日志

### 问题 3：密码错误
**原因**：数据库中的密码不正确
**解决**：
- 重新运行 create_admin.sql
- 确认密码是 admin123

### 问题 4：用户不存在
**原因**：数据库中没有 admin 用户
**解决**：
- 运行创建用户的 SQL 脚本
- 检查 user 表数据

## 📝 检查清单

- [ ] 后端 Spring Boot 运行在 8081 端口
- [ ] MySQL 服务正在运行
- [ ] 数据库 `education_platform` 已创建
- [ ] 执行了 create_admin.sql 脚本
- [ ] 前端 .env 配置为 8081 端口
- [ ] 前端 vite.config.js 代理指向 8081
- [ ] 前端服务已重启
- [ ] 浏览器访问 http://localhost:3000/ 正常

## 💡 如果所有检查都通过但仍然失败

请提供以下信息：
1. 浏览器 F12 Console 的完整错误信息
2. 浏览器 F12 Network 中的请求详情（URL、状态码、响应）
3. 后端控制台的完整日志
4. 数据库查询结果：`SELECT * FROM user WHERE username = 'admin';`
