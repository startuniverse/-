第二步：配置和启动后端 (3分钟)

  2.1 修改配置文件

  打开文件：E:\shixun\backend\src\main\resources\application.yml

  找到以下部分并修改：

  spring:
    datasource:
      url: jdbc:mysql://localhost:3306/education_platform?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
      username: root      # ← 修改为您的MySQL用户名
      password: your_password  # ← 修改为您的MySQL密码

  2.2 启动后端

  方法1：使用Maven命令 (推荐)

  打开命令提示符或PowerShell：

  # 进入后端目录
  cd E:\shixun\backend

  # 首次运行需要下载依赖，可能需要几分钟
  mvn clean compile

  # 启动项目
  mvn spring-boot:run

  方法2：使用IDE (如IntelliJ IDEA)

  1. 打开IDEA
  2. File -> Open -> 选择 E:\shixun\backend 文件夹
  3. 等待Maven依赖下载完成
  4. 找到 EducationPlatformApplication.java
  5. 右键 -> Run 'EducationPlatformApplication'

  2.3 验证后端启动

  当看到类似以下信息时，表示启动成功：
  Started EducationPlatformApplication in X.X seconds

  访问：http://localhost:8080/swagger-ui.html

  您应该能看到API文档页面。

  ---
  第三步：配置和启动前端 (3分钟)

  3.1 安装依赖

  打开新的命令提示符（不要关闭后端）：

  # 进入前端目录
  cd E:\shixun\frontend

  # 安装依赖 (第一次运行需要，可能需要2-5分钟)
  npm install

  如果下载慢，可以使用淘宝镜像：

  npm config set registry https://registry.npmmirror.com
  npm install

  3.2 配置环境变量（可选）

  如果需要修改API地址，编辑 E:\shixun\frontend\.env：

  VITE_API_BASE_URL=http://localhost:8080/api/v1
  VITE_APP_TITLE=城市教育局综合信息服务平台

  3.3 启动前端

  # 在前端目录下
  npm run dev

  启动成功后会显示：
    VITE v4.4.9  ready in XXX ms

     ➜  Local:   http://localhost:3000/
     ➜  Network: use --host to expose

  ---
  第四步：登录使用 (1分钟)

  4.1 访问系统

  打开浏览器访问：http://localhost:3000

  4.2 使用测试账号

  管理员账号（体验全部功能）：
  - 用户名：admin
  - 密码：admin123

  其他角色账号（可选）：
  - 教师：teacher / teacher123
  - 学生：student / student123

  4.3 功能体验

  管理员可以：
  1. 查看管理仪表盘
  2. 管理学生信息
  3. 发布和审批文档
  4. 查看数据分析图表
  5. 管理办公资产

  普通用户可以：
  1. 查看个人信息
  2. 查看课程表
  3. 查询成绩
  4. 查看通知公告

  ---
  🔧 常见问题解决

  问题1：端口被占用

  检查端口占用：
  # Windows
  netstat -ano | findstr :8080
  netstat -ano | findstr :3000

  # 查看进程
  tasklist | findstr "PID"

  解决方法：
  - 修改后端端口：在 application.yml 中修改 server.port
  - 修改前端端口：在 vite.config.js 中修改 server.port

  问题2：数据库连接失败

  检查MySQL是否运行：
  # Windows
  services.msc  # 查看MySQL服务状态

  # Linux
  sudo systemctl status mysql

  检查配置：
  - 确认 application.yml 中的用户名密码正确
  - 确认数据库名是 education_platform

  问题3：Maven依赖下载慢

  使用阿里云镜像：

  编辑 E:\shixun\backend\pom.xml，在 <properties> 前添加：

  <repositories>
      <repository>
          <id>aliyun</id>
          <url>https://maven.aliyun.com/repository/public</url>
          <releases><enabled>true</enabled></releases>
          <snapshots><enabled>false</enabled></snapshots>
      </repository>
  </repositories>

  或者配置全局Maven镜像：编辑 ~/.m2/settings.xml

  问题4：Node.js安装依赖失败

  清除缓存重试：
  cd E:\shixun\frontend
  npm cache clean --force
  rm -rf node_modules package-lock.json
  npm install

  ---
  📊 验证运行状态

  后端验证

  访问：http://localhost:8080/swagger-ui.html
  - ✅ 看到Swagger界面 = 后端正常

  前端验证

  访问：http://localhost:3000
  - ✅ 看到登录页面 = 前端正常

  数据库验证

  mysql -u root -p education_platform -e "SELECT COUNT(*) FROM user;"
  # 应该返回 0 或已添加的用户数

  ---
  🎯 测试数据（可选）

  如果想快速测试，可以插入一些测试数据：

  mysql -u root -p education_platform

  -- 插入管理员用户 (密码: admin123)
  INSERT INTO user (username, password, real_name, status)
  VALUES ('admin', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBa3BjR0v9bYRi', '系统管理员', 1);

  -- 插入管理员角色
  INSERT INTO user_role (user_id, role_id) VALUES (1, 1);

  -- 插入学校
  INSERT INTO school (school_code, school_name, school_type) VALUES ('S001', '实验中学', 'secondary');

  -- 插入班级
  INSERT INTO class (school_id, class_name, class_code, grade) VALUES (1, '三年级一班', 'C001', '三年级');

  ---
  📝 完整运行流程总结

  1. 启动MySQL
     ↓
  2. 创建数据库 education_platform
     ↓
  3. 导入 database/schema.sql
     ↓
  4. 修改 backend/application.yml (数据库密码)
     ↓
  5. cd backend && mvn spring-boot:run
     ↓
  6. (新终端) cd frontend && npm install
     ↓
  7. npm run dev
     ↓
  8. 浏览器访问 http://localhost:3000
     ↓
  9. 登录: admin / admin123

  ---
  💡 快速启动脚本（可选）

  创建一个批处理文件 E:\shixun\start.bat：

  @echo off
  echo 启动教育局平台...

  echo 1. 启动后端...
  cd /d E:\shixun\backend
  start "Backend" mvn spring-boot:run

  echo 2. 等待10秒...
  timeout /t 10 /nobreak

  echo 3. 启动前端...
  cd /d E:\shixun\frontend
  start "Frontend" npm run dev

  echo 完成！请访问 http://localhost:3000
  pause

  双击运行即可！

  ---
  🆘 遇到问题？

  1. 查看日志：
    - 后端日志：控制台输出
    - 前端日志：浏览器F12开发者工具
  2. 检查配置：
    - 数据库连接配置
    - 端口配置
    - 环境变量
  3. 重启服务：
    - 停止所有服务
    - 重新启动

  需要更详细的帮助吗？我可以针对具体问题提供解决方案！
