# 快速测试方法

## 🔥 最快的诊断方式

### 方法 1：浏览器直接测试后端接口

在浏览器中访问这个 URL（GET 请求）：
```
http://localhost:8081/api/v1/auth/info
```

**预期结果**：
- 如果返回 401 或错误信息 → 说明后端在运行，但需要 Token
- 如果返回 404 → 说明路径错误或后端没运行
- 如果无法访问 → 说明后端没启动

### 方法 2：使用浏览器开发者工具

1. 打开 F12 → Console 标签
2. 在登录页面输入 admin/admin123
3. 点击登录
4. 在 Console 中应该看到详细的错误信息

### 方法 3：查看 Network 请求

1. 打开 F12 → Network 标签
2. 勾选 "Preserve log"
3. 点击登录
4. 找到登录请求（应该是红色的）
5. 点击该请求，查看：
   - **Request URL**: 应该是 `http://localhost:8081/api/v1/auth/login`
   - **Status**: 状态码
   - **Response**: 后端返回的具体错误信息

## 🎯 可能的错误类型

### 错误 1：404 Not Found
**原因**：
- 后端没运行
- 端口错误（应该是 8081）
- 路径错误

**解决**：
```bash
# 检查后端是否运行
netstat -ano | findstr :8081

# 如果没运行，启动后端
cd E:\shixun\backend
mvn spring-boot:run
```

### 错误 2：500 Internal Server Error
**原因**：
- 数据库连接失败
- SQL 查询错误
- 密码验证失败

**查看详细错误**：
在浏览器 Console 中查看完整的错误响应。

### 错误 3：返回 code: 500, message: "用户不存在"
**原因**：
- 数据库中没有 admin 用户

**解决**：
```sql
-- 在 MySQL 中执行
USE education_platform;
INSERT INTO user (username, password, real_name, status, deleted)
VALUES ('admin', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBa3BjR0v9bHri', '管理员', 1, 0);
```

### 错误 4：返回 code: 500, message: "密码错误"
**原因**：
- 数据库中的密码加密值不正确

**解决**：
```sql
-- 更新正确的密码（admin123）
UPDATE user
SET password = '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBa3BjR0v9bHri'
WHERE username = 'admin';
```

### 错误 5：返回 code: 500, message: "用户已被禁用"
**原因**：
- 用户 status 字段不是 1

**解决**：
```sql
UPDATE user SET status = 1 WHERE username = 'admin';
```

## 📋 一键诊断命令

在 PowerShell 中运行：

```powershell
Write-Host "=== 1. 检查后端端口 ==="
netstat -ano | findstr :8081

Write-Host "`n=== 2. 检查数据库用户 ==="
mysql -u root -p education_platform -e "SELECT id, username, status FROM user WHERE username = 'admin';"

Write-Host "`n=== 3. 测试登录接口 ==="
Invoke-WebRequest -Uri "http://localhost:8081/api/v1/auth/login" -Method POST -ContentType "application/json" -Body '{"username":"admin","password":"admin123"}'
```

## 💡 终极解决方案

如果以上都失败，执行这个完整的重置：

```sql
-- 在 MySQL 中执行
USE education_platform;

-- 清理旧数据
DELETE FROM user_role;
DELETE FROM user;
DELETE FROM role;

-- 插入基础数据
INSERT INTO role (id, role_code, role_name, description, status, deleted)
VALUES (1, 'ADMIN', '系统管理员', '最高权限', 1, 0);

INSERT INTO user (id, username, password, real_name, status, deleted)
VALUES (1, 'admin', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBa3BjR0v9bHri', '管理员', 1, 0);

INSERT INTO user_role (user_id, role_id) VALUES (1, 1);

-- 验证
SELECT * FROM user;
SELECT * FROM user_role;
```

然后重启前端服务：
```bash
cd E:\shixun\frontend
npm run dev
```

## 🎯 立即告诉我

请运行以下命令并把结果发给我：

```bash
# 1. 检查后端
netstat -ano | findstr :8081

# 2. 检查数据库
mysql -u root -p education_platform -e "SELECT id, username, status, password FROM user WHERE username = 'admin';"

# 3. 测试接口
curl -X POST http://localhost:8081/api/v1/auth/login -H "Content-Type: application/json" -d "{\"username\":\"admin\",\"password\":\"admin123\"}"
```

这样我就能准确知道问题所在！
